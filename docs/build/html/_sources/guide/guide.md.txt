# User Guide

The SONICDB package delivers two main classes: `sonic` and `audio`.

## SONIC

The `sonic` class is the main class that manages the database connection and provides methods for creating, updating, and deleting records in the database. 


